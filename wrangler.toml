name = "dogdle"
main = "src/worker.js"
compatibility_date = "2025-06-01"

[assets]
directory = "public"
binding = "ASSETS"
run_worker_first = ["/api/*"]

# Once you're ready to go live on your domain, add a route, e.g.:
# routes = [{ pattern = "dogdle.swampkat.com", custom_domain = true }]
